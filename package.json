{
  "name": "task-board-app",
  "version": "1.0.0",
  "description": "Full-stack Task Board Web Application",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
